# Lab 1 — Task Triage CLI Agent

Learning goal: the `tool_use → tool_result → loop` cycle of the Anthropic SDK.

## Run

```bash
cp .env.example .env           # fill in ANTHROPIC_API_KEY
cp tasks.example.md tasks.md   # edit with your tasks
npm install
npm run triage
```

## Status

In progress.
